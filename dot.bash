SOURCE_PATH=$(pwd);

ln -s $SOURCE_PATH/ackrc $HOME/.ackrc;
ln -s $SOURCE_PATH/atom/config.cson $HOME/.atom/config.cson
ln -s $SOURCE_PATH/atom/keymap.cson $HOME/.atom/keymap.cson
ln -s $SOURCE_PATH/atom/styles.less $HOME/.atom/styles.less
ln -s $SOURCE_PATH/bash_aliases $HOME/.bash_aliases;
ln -s $SOURCE_PATH/bash_profile $HOME/.bash_profile;
ln -s $SOURCE_PATH/bashrc $HOME/.bashrc;
ln -s $SOURCE_PATH/gemrc $HOME/.gemrc;
ln -s $SOURCE_PATH/inputrc $HOME/.inputrc;
ln -s $SOURCE_PATH/irbrc $HOME/.irbrc;
ln -s $SOURCE_PATH/pryrc $HOME/.pryrc;
